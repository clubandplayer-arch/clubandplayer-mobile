# Reporting Notes

This repository includes a workflow note for automated agents:

- When requested by the user, summaries can include full file contents for every changed file.
- Keep regular engineering checks (lint/tests) separate from summary formatting.
- Prefer concise, traceable commits with clear PR descriptions.
